import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nihiltheism.corpus',
  appName: 'Nihiltheism Corpus',
  webDir: 'dist'
};

export default config;
